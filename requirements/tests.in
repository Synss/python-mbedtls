# Base
cython
pytest

# Tooling
coverage
readme_renderer < 42.0
# 42.0 requires nh3, which is a rust dependency.  Installing cargo/rust
#      in a container is too much work for now.

# QA
pre-commit
pylint

# Plugins
pylint-per-file-ignores
pytest-cov != 2.9.0
pytest-repeat
pytest-timeout

# Broken deps
# https://github.com/yaml/pyyaml/issues/601
pyyaml != 6.0.0
